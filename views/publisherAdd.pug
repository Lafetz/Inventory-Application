extends layout

block content
  form(action="#", method="post") 
   div
    label title 
     input(type="text" name='name' ) 
  
    if(typeof errors !== "undefined" && errors.length!==0)
     p #{errors[0].msg}
  
   button Add  